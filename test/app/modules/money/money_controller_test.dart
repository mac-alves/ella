import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/money_controller.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // MoneyController money;
  //
  setUp(() {
    //     money = MoneyModule.to.get<MoneyController>();
  });

  group('MoneyController Test', () {
    //   test("First Test", () {
    //     expect(money, isInstanceOf<MoneyController>());
    //   });

    //   test("Set Value", () {
    //     expect(money.value, equals(0));
    //     money.increment();
    //     expect(money.value, equals(1));
    //   });
  });
}
