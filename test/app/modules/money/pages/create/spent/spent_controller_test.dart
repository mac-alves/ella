import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/pages/create/spent/spent_controller.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // SpentController spent;
  //
  setUp(() {
    //     spent = MoneyModule.to.get<SpentController>();
  });

  group('SpentController Test', () {
    //   test("First Test", () {
    //     expect(spent, isInstanceOf<SpentController>());
    //   });

    //   test("Set Value", () {
    //     expect(spent.value, equals(0));
    //     spent.increment();
    //     expect(spent.value, equals(1));
    //   });
  });
}
