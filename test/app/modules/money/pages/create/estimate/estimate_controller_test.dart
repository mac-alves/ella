import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/pages/create/estimate/estimate_controller.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // EstimateController estimate;
  //
  setUp(() {
    //     estimate = MoneyModule.to.get<EstimateController>();
  });

  group('EstimateController Test', () {
    //   test("First Test", () {
    //     expect(estimate, isInstanceOf<EstimateController>());
    //   });

    //   test("Set Value", () {
    //     expect(estimate.value, equals(0));
    //     estimate.increment();
    //     expect(estimate.value, equals(1));
    //   });
  });
}
