import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ella/app/modules/money/pages/create/estimate/estimate_page.dart';

void main() {
  testWidgets('EstimatePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EstimatePage(title: 'Estimate')));
    //  final titleFinder = find.text('Estimate');
    //  expect(titleFinder, findsOneWidget);
  });
}
