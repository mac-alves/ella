import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MoneyPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MoneyPage(title: 'Money')));
    //  final titleFinder = find.text('Money');
    //  expect(titleFinder, findsOneWidget);
  });
}
