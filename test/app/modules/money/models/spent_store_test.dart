import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/models/spent_store.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // SpentStore spent;
  //
  setUp(() {
    //     spent = MoneyModule.to.get<SpentStore>();
  });

  group('SpentStore Test', () {
    //   test("First Test", () {
    //     expect(spent, isInstanceOf<SpentStore>());
    //   });

    //   test("Set Value", () {
    //     expect(spent.value, equals(0));
    //     spent.increment();
    //     expect(spent.value, equals(1));
    //   });
  });
}
