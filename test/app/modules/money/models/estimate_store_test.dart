import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/models/estimate_store.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // EstimateStore estimate;
  //
  setUp(() {
    //     estimate = MoneyModule.to.get<EstimateStore>();
  });

  group('EstimateStore Test', () {
    //   test("First Test", () {
    //     expect(estimate, isInstanceOf<EstimateStore>());
    //   });

    //   test("Set Value", () {
    //     expect(estimate.value, equals(0));
    //     estimate.increment();
    //     expect(estimate.value, equals(1));
    //   });
  });
}
