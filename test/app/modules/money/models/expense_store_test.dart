import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/money/models/expense_store.dart';
import 'package:ella/app/modules/money/money_module.dart';

void main() {
  initModule(MoneyModule());
  // ExpenseStore expense;
  //
  setUp(() {
    //     expense = MoneyModule.to.get<ExpenseStore>();
  });

  group('ExpenseStore Test', () {
    //   test("First Test", () {
    //     expect(expense, isInstanceOf<ExpenseStore>());
    //   });

    //   test("Set Value", () {
    //     expect(expense.value, equals(0));
    //     expense.increment();
    //     expect(expense.value, equals(1));
    //   });
  });
}
