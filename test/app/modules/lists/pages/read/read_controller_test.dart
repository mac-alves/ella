import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/lists/pages/read/read_controller.dart';
import 'package:ella/app/modules/lists/lists_module.dart';

void main() {
  initModule(ListsModule());
  // ReadController read;
  //
  setUp(() {
    //     read = ListsModule.to.get<ReadController>();
  });

  group('ReadController Test', () {
    //   test("First Test", () {
    //     expect(read, isInstanceOf<ReadController>());
    //   });

    //   test("Set Value", () {
    //     expect(read.value, equals(0));
    //     read.increment();
    //     expect(read.value, equals(1));
    //   });
  });
}
