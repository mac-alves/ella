import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ella/app/modules/lists/pages/read/read_page.dart';

void main() {
  testWidgets('ReadPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReadPage(title: 'Read')));
    //  final titleFinder = find.text('Read');
    //  expect(titleFinder, findsOneWidget);
  });
}
