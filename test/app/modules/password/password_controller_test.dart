import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/password/password_controller.dart';
import 'package:ella/app/modules/password/password_module.dart';

void main() {
  initModule(PasswordModule());
  // PasswordController password;
  //
  setUp(() {
    //     password = PasswordModule.to.get<PasswordController>();
  });

  group('PasswordController Test', () {
    //   test("First Test", () {
    //     expect(password, isInstanceOf<PasswordController>());
    //   });

    //   test("Set Value", () {
    //     expect(password.value, equals(0));
    //     password.increment();
    //     expect(password.value, equals(1));
    //   });
  });
}
