import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/password/models/password_store.dart';
import 'package:ella/app/modules/password/password_module.dart';

void main() {
  initModule(PasswordModule());
  // PasswordStore password;
  //
  setUp(() {
    //     password = PasswordModule.to.get<PasswordStore>();
  });

  group('PasswordStore Test', () {
    //   test("First Test", () {
    //     expect(password, isInstanceOf<PasswordStore>());
    //   });

    //   test("Set Value", () {
    //     expect(password.value, equals(0));
    //     password.increment();
    //     expect(password.value, equals(1));
    //   });
  });
}
