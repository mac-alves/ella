import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ella/app/modules/password/pages/create/create_page.dart';

void main() {
  testWidgets('CreatePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CreatePage(title: 'Create')));
    //  final titleFinder = find.text('Create');
    //  expect(titleFinder, findsOneWidget);
  });
}
