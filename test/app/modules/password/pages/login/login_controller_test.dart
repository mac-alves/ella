import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/modules/password/pages/login/login_controller.dart';
import 'package:ella/app/modules/password/password_module.dart';

void main() {
  initModule(PasswordModule());
  // LoginController login;
  //
  setUp(() {
    //     login = PasswordModule.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
