import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ella/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ListsController lists;
  //
  setUp(() {
    //     lists = AppModule.to.get<ListsController>();
  });

  group('ListsController Test', () {
    //   test("First Test", () {
    //     expect(lists, isInstanceOf<ListsController>());
    //   });

    //   test("Set Value", () {
    //     expect(lists.value, equals(0));
    //     lists.increment();
    //     expect(lists.value, equals(1));
    //   });
  });
}
